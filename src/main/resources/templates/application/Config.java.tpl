package ${BASE_PACKAGE}.${MODULE}.application.config;

import ${BASE_PACKAGE}.${MODULE}.domain.spi.${MODULE_CAP}Repository;
import ${BASE_PACKAGE}.${MODULE}.domain.usecase.${MODULE_CAP}UseCase;
import ${BASE_PACKAGE}.${MODULE}.domain.usecase